# Sprint AI/ML — 13/08/2026

## Đã hoàn thành

- Tạo Evaluation Lab gồm 60 câu tiếng Việt do dự án tự soạn, gắn nhãn trước khi gọi model. Không có dữ liệu tài chính thật, dữ liệu người dùng hoặc bản ghi giọng nói.
- Thêm runner Gemini 2.5 Flash, fixture không gọi API, metrics theo trường và báo cáo JSON/Markdown.
- Chạy evaluation thật với prompt `text-extraction-v2`: 60/60 response parse được; exact transaction accuracy 83,64%; từ chối 5/5 câu ngoài phạm vi; JSON invalid 0%.
- Publish report thật lên AI Quality Lab trong ứng dụng. Script từ chối publish fixture.
- Thêm Data Quality Guard cho tổng tiền, phép nhân, ngày tương lai và outlier theo lịch sử cùng mặt hàng. Guard chỉ cảnh báo; không tự sửa hoặc tự lưu giao dịch.
- Thêm evidence 7 ngày vào daily insight. Gemini chỉ nhận số liệu tổng hợp do code tính, không nhận từng giao dịch.
- Chuẩn hóa manifest TTS synthetic thành 30 mẫu và runner audio dùng phiên Firebase tạm có xóa sau khi chạy.

## Kiểm thử

- `npm run eval:text:fixture`
- `npm run eval:metrics`
- Regression schema Gemini, extraction, Data Quality Guard, insight 7 ngày và UI evaluation.
- `npm run lint`
- `npm run build`

## Giới hạn và quyết định

- Evaluation text synthetic không đại diện cho cách nói ngoài chợ thật. Không được gọi đây là “voice accuracy”.
- TTS synthetic chưa tạo được audio trên API key hiện tại: Gemini TTS trả `content_blocked` cho request thử. Không có audio nào được lưu, không có metric voice nào được công bố.
- Voice extraction vẫn bắt buộc màn hình xác nhận trước khi lưu; không hạ chuẩn an toàn để lấy metric.

## Việc tiếp theo

1. Khi Gemini TTS/API policy khả dụng, chạy 30 synthetic WAV rồi `run-audio-eval.mjs` và scorer để tạo report voice.
2. Nếu cần negative control public, tự tải Common Voice Vietnamese theo license CC0, tạo manifest chỉ từ path clip và transcript công khai, không dùng `client_id`.
3. Chạy external audit trên điện thoại và ghi kết quả quan sát tách khỏi evaluation model.
