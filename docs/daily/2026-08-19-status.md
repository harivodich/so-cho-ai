# Audit sprint AI/ML — 19/08/2026

## Kết quả đã xác minh

- `npm run verify:evidence`: pass; 60 mẫu text synthetic và 30 mẫu FLEURS public negative-control.
- `npm test`: 26 test file, 60 test pass.
- `npm run lint`: pass.
- Production build Next.js: pass.
- Bản public Vercel `https://so-cho-ai-tau.vercel.app`: trang chủ HTTP 200.
- `/api/firebase-config` chỉ trả public Firebase web config; bốn trường bắt buộc đều có.
- `/api/extract` và `/api/insights` trả HTTP 401 nếu không có Firebase ID token.
- Firestore rules giới hạn dữ liệu theo `request.auth.uid`.

## Bằng chứng AI/ML

- Text extraction dùng `gemini-2.5-flash`, prompt version `text-extraction-v2`.
- Data Quality Guard chạy sau Zod và trước màn hình xác nhận; không tự sửa hoặc tự lưu.
- Insight 7 ngày chỉ gửi aggregate do code tính sang Gemini, không gửi raw transaction, UID, audio hoặc transcript.
- FLEURS chỉ là negative-control public speech; không được diễn giải thành transaction-voice accuracy.
- Chưa công bố transaction-audio accuracy vì TTS tiếng Việt bị `content_blocked`.

## Còn cần kiểm tra thủ công trước khi nộp

1. Firebase Anonymous Auth, lưu giao dịch, tải lại trang và cách ly hai UID trong cửa sổ ẩn danh.
2. Voice → Gemini → màn hình xác nhận → sửa/lưu; không lưu audio lâu dài.
3. Fallback nhập tay khi microphone, quota hoặc Gemini lỗi.
4. Quay video và kiểm tra toàn bộ link ở chế độ ẩn danh.

## Giới hạn công bố

Không dùng metric synthetic/public để tuyên bố độ chính xác cho toàn bộ tiểu thương chợ thực tế. Mọi giao dịch AI đều phải qua xác nhận của người dùng.
