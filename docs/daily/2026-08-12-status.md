# Báo cáo ngày 12/08/2026 — Dashboard, tăng trưởng và voice evaluation

## Đã hoàn thành

- Báo cáo ngày, 7 ngày, tháng và khoảng tùy chọn; bộ lọc loại/mặt hàng, 6 KPI, biểu đồ doanh thu và top mặt hàng.
- Mục tiêu doanh thu tháng lưu cục bộ, tiến độ, số còn lại, trung bình/ngày và trung tâm việc cần làm tính bằng code.
- Nhận xét AI cuối ngày chỉ gửi aggregate, Structured Output + Zod, Firebase auth, giới hạn body 4 KB và quota 5 lượt/ngày.
- Hỏi nhanh về doanh thu/lãi/đơn trung bình không dùng AI.
- Export CSV theo kỳ/bộ lọc, có phòng chống CSV formula injection.
- Nguồn tra giá công khai Agro.gov.vn được gắn dưới dạng tham khảo; không tự đề xuất giá.
- Voice cho phép mảng rỗng khi im lặng/ngoài phạm vi, tránh ép model tạo giao dịch giả.
- Evaluation gồm 20 mẫu synthetic TTS và công cụ chọn 10 mẫu public âm tính từ Common Voice Vietnamese 25.0; runner gọi đúng API app.

## Kiểm thử đã chạy

```text
npm test                                  # 28/28 passed trước hardening body limit
npm run lint                              # passed
npm run build                             # passed; /api/insights được tạo
node --check evaluation/scripts/*.mjs     # 5 script passed
score fixture                             # transaction + non-transaction: 100% fixture
Smoke GET /                               # 200
Smoke POST /api/insights không token      # 401
Browser QA desktop/mobile                 # không overflow, không lỗi mã hóa
Impeccable detector                       # 0 finding trên các file UI mới
```

## Chưa chạy vì cần quyền/chi phí bên ngoài

- Chưa gọi 20 lượt Gemini TTS và 20 lượt extraction.
- Chưa tải Common Voice Vietnamese 25.0 và chạy 10 mẫu public âm tính.
- Chưa deploy Cloud Run/release; không tự deploy khi chưa được yêu cầu.

## Cổng tiếp theo

1. Xác nhận billing/quota cho tối đa 40 lượt Gemini và tải Common Voice từ nguồn CC0 chính thức.
2. Chạy hai manifest, lưu model/prompt version và kết quả thật; không xóa mẫu lỗi.
3. Chỉ mở nhập ảnh nếu voice đạt ngưỡng đã khóa; nếu không, tiếp tục sửa voice và giữ ảnh ngoài MVP.
